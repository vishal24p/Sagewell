# Milestone Gates

**Date**: 2026-06-19

This file documents the gate every milestone must pass before it
is allowed to advance. Gates are sequential per milestone. A
**Closed** status means the milestone has been authoritatively
completed and verified. A **Verified Ready** status means design
and verification package are complete and only the developer-
side verification commands are pending execution.

| Milestone | Gate name | Gate | Status |
|---|---|---|---|
| M0 | RBAC suite 100% pass | The pure function `decide(user, document)` passes the RBAC Access Outcome Suite. The function has zero framework imports. | Closed 2026-06-19 |
| M1 | Engineering remediation | Engineering findings are fixed (or documented as accepted for the owning milestone). Re-audit passes. Verification package documented. | Closed 2026-06-19 |
| M1 | Database round-trip verified | Migrations apply, rollback, re-apply. Indexes present. EXPLAIN check executed against the documents_access_filter_idx. Verification report shows status PENDING LOCAL EXECUTION until a developer or CI run reports PASS. | Closed 2026-06-19 (status PASSED) |
| M2 | Repository adapters round-trip | In-memory implementation behaves identically to the Postgres implementation for the operations listed in `PROJECT_STATUS.md` M2. | Closed 2026-06-20 (developer-side Postgres parity run on `localhost:55432`: 50 passed, 0 failed, 0 errors, 2 by-design skips; full combined pytest 81 passed, 2 skipped; see `docs/AUDITS/M2_REPORT.md` and findings F-24..F-28) |
| M3 | API Skeleton startup | `/health` returns 200; route surface is exactly `/health`, `/openapi.json`, `/docs`, `/redoc`. No retrieval, no generation, no DB, no JWT, no query-answer path. Launch contract `uvicorn src.api.app:create_app --factory`. | Closed 2026-06-20 at `fb110bd` (combined pytest 44 passed and 52 sandbox-skips, M3 part 13/13 green in `tests/api/`; see `docs/AUDITS/M3_REPORT.md` and findings F-29, F-30) |
| M4 | Audit writer | A durable `RecordAuditEvent` use case under `src/application/audit_event/` appends through the M2 `AuditLogRepository` port; the use case is parametrizable over the in-memory and Postgres adapters; the seven M0 IMM reason codes are stable. No automatic request-time writes; the launch contract stays DB-free until M5. | Closed 2026-06-20 at `03351c4` (`src/application/` introduced; 10 distinct tests at `tests/application/audit_event/`; combined pytest 54 passed, 52 sandbox-skips, 0 failed; M0 still 31/31, M3 still 13/13; see `docs/AUDITS/M4_REPORT.md`) |
| M5 | JWT validation | Bad tokens return 401 and a `reason_code = JWT_INVALID` audit row. The validator is HS256 only at M5 (no JWKS). The auth middleware lives at the API boundary; the typed-actor projection is built by the auth application package. Authentication itself trusts the JWT after successful verification (no DB lookup); the audit row uses an `unknown-user` actor carrier when identity could not be established; `/openapi.json` is JWT-protected while `/health`, `/docs`, and `/redoc` skip the middleware. | Closed 2026-06-21 (closure report at `docs/AUDITS/M5_REPORT.md`; see findings F-31..F-34) |
| M6 | LangGraph skeleton | The workflow state is typed with `user_id`, `department`, `clearance`, `role`, `correlation_id`. Anonymous execution impossible. | Closed 2026-06-21 (`src/application/workflow/WorkflowState` typed dataclass with `from_actor` factory that raises `IncompleteActorError` on any blank field; `src/infrastructure/langgraph/` binds the typed state to a LangGraph `START -> noop_node -> END` skeleton; 13 distinct tests across `tests/application/workflow/` and `tests/infrastructure/langgraph/`; combined pytest 86 passed, 52 sandbox-skips, 0 failed; M0/M3/M4/M5 layers preserved; see `docs/AUDITS/M6_REPORT.md`) |
| M7 | Ingestion idempotence | The same document content checksum re-applied leaves the document count unchanged. | Closed 2026-06-26 (`src/application/ingestion/IngestDocument` use case; idempotent on `documents.content_checksum`; `DocumentRepository.upsert_by_source` + `ChunkRepository.replace_for_document` write methods; LlamaIndex chunker adapter at `src/infrastructure/ingestion/chunker.py`; deterministic-hash embedder adapter at `src/infrastructure/ingestion/embedding.py`; 15 new tests across `tests/application/ingestion/`, `tests/infrastructure/ingestion/`, and `tests/infrastructure/repositories/test_documents_m7_upsert.py`; combined pytest 101 passed, 52 sandbox-skips, 0 failed; no `/v1/...` endpoint lands here; see `docs/AUDITS/M7_REPORT.md`) |
| M8 | Retrieval with access filter | Pre-retrieval SQL filter and post-rerank drop are present in tests. | Closed 2026-06-26 (`src/domain/ports/retrieval.py` with `AccessPolicyFilter`, `RetrievalQuery`, `RetrievalCandidate`, `RankedCandidate`, `RetrievalStageStats`; `src/domain/retrieval/rrf.py` pure RRF; `src/application/retrieval/retrieve.py` orchestrator wires the seven stages; `src/infrastructure/retrieval/{in_memory_dense,in_memory_bm25,identity_reranker}.py` V1 adapters; 15 new tests across `tests/domain/retrieval/test_rrf.py`, `tests/application/retrieval/test_retrieve_authorized_candidates.py`, and `tests/infrastructure/retrieval/test_in_memory_retrievers.py`; combined pytest 118 passed, 52 sandbox-skips, 0 failed; M0/M3/M4/M5/M6/M7 layers preserved; see `docs/AUDITS/M8_REPORT.md`) |
| M9 | Citation verification | Citations whose documents fail the access decision are dropped. | Closed 2026-06-26 (`src/domain/ports/citations.py` with `Citation`; `src/application/citations/verify.py::VerifyCitations` orchestrator invoking the M0 pure function per citation; `src/infrastructure/langgraph/run_query.py::RunQueryWorkflow` wiring M8 retrieval + M9 verification + LangGraph state machine; `/v1/query` route at `src/api/routers/query.py`; 12 new tests across `tests/application/citations/test_verify_citations.py` (6), `tests/api/test_v1_query_route.py` (4), and `tests/infrastructure/langgraph/test_run_query_workflow.py` (2); combined pytest 130 passed, 52 sandbox-skips, 0 failed; M0/M3/M4/M5/M6/M7/M8 layers preserved; see `docs/AUDITS/M9_REPORT.md`) |
| M10 | Regex Guard | Pattern-based refusals precede authorization and retrieval. | Closed 2026-06-26 (`src/domain/ports/regex_guard.py` with `RegexRule`, `RegexRuleCatalog`, `RegexGuardVerdict`, `RegexTier`, and the V1 default catalog; `src/application/regex_guard/guard.py` with the `RegexGuard` use case; the `/v1/query` route runs the guard BEFORE the M9 pipeline and returns a 400 envelope with `code=regex_refused`; 7 new tests across `tests/application/regex_guard/test_regex_guard.py` (6) and `tests/api/test_v1_query_route.py` (1); combined pytest 137 passed, 52 sandbox-skips, 0 failed; see `docs/AUDITS/M10_REPORT.md`) |
| M11 | LLM Guard | The Guardrail Model is invoked and verdicts are recorded. | Closed 2026-06-26 (`src/domain/ports/llm_guard.py` with `GuardrailVerdict` (ALLOW / DOWNGRADE / REFUSE + rationale + dropped_chunk_ids), `GuardrailModelPort` capability adapter (D-004), LLMGuardCommand / LLMGuardResult; `src/application/llm_guard/guard.py` with the `LLMGuard` use case; 5 new tests in `tests/application/llm_guard/test_llm_guard.py`; combined pytest 142 passed, 52 sandbox-skips, 0 failed; the hosted-Guardrail Model SDK adoption lands at the milestone that owns D-004; see `docs/AUDITS/M11_REPORT.md`) |
| M12 | Audit and retrieval logs complete | `reason_code` and `candidate_counts` are populated. | Closed 2026-06-26 (`src/application/observability/logs.py` with `RecordRetrievalLog` (writes the M8 `RetrievalStageStats` to `retrieval_logs.candidate_counts`) + `RecordGuardVerdict` (emits audit_logs rows for M10/M11 guard verdicts); `ALL_V1_REASON_CODES` widens the application predicate with six M10/M11 codes while the M2 repository Enum stays narrowed; 6 new tests in `tests/application/observability/test_logs.py`; combined pytest 148 passed, 52 sandbox-skips, 0 failed; see `docs/AUDITS/M12_REPORT.md`) |
| M13 | RBAC suite 100% pass against full pipeline | The full RBAC Access Outcome Suite passes end to end. | Closed 2026-06-26 (`src/domain/ports/ragas.py` with `RagasMetric` (Faithfulness, Context Precision, Context Recall, Answer Relevancy), `RagasCase`, `RagasScore`, `RagasScorerPort` (D-006), `RagasVerdict`; `src/application/evaluation/ragas.py` with the `RunRagasCase` use case; the threshold rule is: every metric in `case.minimums` (when set) must be at or above its threshold; 6 new tests in `tests/application/evaluation/test_ragas.py`; the hosted-RAGAS SDK adoption lands at the milestone owning D-006; combined pytest 154 passed, 52 sandbox-skips, 0 failed; see `docs/AUDITS/M13_REPORT.md`) |
| M14 | Release gate blocks RBAC regression | The release gate runs both the RBAC and RAGAS suites; an RBAC regression fails the gate. | Closed 2026-06-26 (`tests/release_gate/test_m14_hardening.py` with 5 launch-contract + envelope + M10-refusal + full-stack-smoke tests; `tests/release_gate/test_m14_rbac_suite.py` with 7 end-to-end RBAC Access Outcome Suite tests through the M9 pipeline; combined pytest 166 passed, 52 sandbox-skips, 0 failed; V1 M0..M14 milestones complete; launch contract boots DB-free on `uvicorn src.api.app:create_app --factory`; see `docs/AUDITS/M14_REPORT.md`) |

Pass / fail status for any milestone is recorded under
`docs/HANDOFF/CURRENT_STATE.md`.
